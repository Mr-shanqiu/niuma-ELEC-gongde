# 隐私说明

牛马电子功德仅统计以下事件：

- 键盘按下（key down）
- 鼠标按键按下（left/right/middle/x-button）
- 滚动手势（连续滚轮底层事件合并为一次操作）

产品不会读取、保存、上传：

- 按键内容或键码
- 鼠标坐标
- 当前窗口、活动应用或标题
- 剪贴板
- 截图

形象包说明

- 应用仅在用户主动双击导入或选择形象时读取本机 `.nmgpack`、`manifest.json` 和 PNG 图片。
- 形象包是纯数据文件，不能修改应用功能，也不能包含脚本、动态库或网络地址。
- 已安装形象保存在本机应用支持目录，不会上传或同步。

数据存储说明

- 总数、窗口位置、隐私说明确认状态和自启动设置保存在本机。
- 不会上报到开发者服务器。
- 仅在本地每 5 秒及退出时落盘。

权限说明

- macOS：启动时展示本地说明，说明“输入监控”权限范围与应用用途；用户同意后请求系统权限。
- Windows：启动时展示一次本地隐私说明，不涉及系统键盘/鼠标内容权限。
- 如果用户未授予 macOS 输入监控权限，应用不会进行全局计数；用户可从右键菜单主动再次开启。
- 首次运行默认建立当前用户的登录启动项，用户可从右键菜单随时关闭；该功能不需要管理员权限。

版本边界

- 本版本不包含广告、遥测、版本更新检查、账号、支付、上传与任何联网功能。
- 官方可统计的仅是“官方下载链接请求量”，不能代表真实安装/启动/活跃用户数。

## 本地形象包边界（macOS / Windows 0.6.0）

- 用户主动导入的 `.nmgpack` 只保存在本机应用支持目录。
- 客户端只读取经过严格校验的 JSON 动画参数和 PNG 图片，不包含或执行脚本、动态库、网址及其他程序功能。
- 形象包不会上传；macOS 和 Windows 0.6.0 均不包含账号、支付、用户上传、广告、遥测、更新检查或任何联网功能。
